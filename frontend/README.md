# Frontend (Dashboard)

This directory is reserved for the backtest result dashboard UI.

Current state:
- Structure and boundary documents only (Task 074).
- No implemented screens/components yet.

Stack boundary decision for current planning:
- Preferred default: Next.js + React.
- Do not mix Next.js app scaffolding with Vite bundling in the same frontend app.
- A Vite + React + TypeScript SPA is an alternative only if explicitly selected in a future task.

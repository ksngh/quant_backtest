# Task 074: BACKEND_FRONTEND_DIRECTORY_AND_AGENTS_BOUNDARIES

## Goal
Create backend/frontend area boundaries and scaffolding for dashboard work.

## Required
- Add `backend/` and `frontend/` base directories.
- Add focused `AGENTS.md` and `STATUS.md` for each area.
- Define routing/ownership boundaries in root guidance.

## Safety
- Keep read-only dashboard boundary.
- No auth/login/live trading/order execution.

## Acceptance
- Directory boundaries exist and documented.

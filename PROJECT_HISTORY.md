# Project History (Recent Window)

This file keeps recent completion context only.
Older history is preserved in fixed 50-task segmented archives:

- `docs/ledger_archives/project_history_task_001_050.md`
- `docs/ledger_archives/project_history_task_051_100.md`
- `docs/ledger_archives/project_history_task_101_150.md`

## Recent Completion Window (Tasks 151-187)
- 2026-05-24: Completed Task 151 `ENTRY_FILL_EQUITY_AND_CONSERVATIVE_SIZING_SEMANTICS`; corrected strategy-engine primary equity so newly opened entry candles use execution-price valuation, added conservative max(execution price, close) sizing for cash-fraction/target-notional entries, propagated equity valuation metadata to persistence, updated API/frontend display semantics, and verified with targeted backtesting/CLI/persistence tests, frontend build, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 152 `PATTERN_SIZING_PROPAGATION_CONTRACT`; pattern lifecycle entries now use `quantity=None` by default so engine sizing owns fixed/cash-fraction/target-notional quantities, explicit pattern quantity overrides propagate through `_expand_raw_actions()` into `build_pattern_trade_actions()`, and sizing-source metadata is emitted. Verified with targeted builder/CLI/engine tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 153 `PARTIAL_EXIT_QUANTITY_RATIO_CONTRACT`; added `StrategyQuantityMode`, made pattern builder exit actions carry position-ratio quantity semantics, resolved ratio exits against current open position in the strategy engine, blocked invalid ratios deterministically, and preserved ratio/resolved quantity metadata in persistence/graph markers. Verified with targeted engine/builder/persistence/action tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 154 `ENTRY_FILL_PRICE_MODEL_SEPARATION`; canonical expansion now passes the actual confirmation candle into `build_pattern_trade_actions()`, market-on-confirmation fills use actual close instead of a synthetic entry-reference candle, reference/limit modes remain explicit and require OHLC touches, and fill source/assumption metadata is emitted. Verified with targeted builder/entry/runner/engine tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 155 `RISK_PLAN_INVALID_ACTION_SAFETY`; reusable pattern action builder now returns only `SKIP` diagnostics for invalid or skipped risk plans, preserves risk plan status/reasons in metadata, and no longer emits executable entries for invalid plans. Verified with targeted builder/CLI tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 156 `CANONICAL_SOFT_INVALIDATION_INTEGRATION`; added active `pattern_invalidation` soft-rule builder, propagated pattern-specific fields through strategy metadata, wired canonical expansion to pass close-based invalidation rules into pattern exit simulation, and covered FVG/trendline/neckline rules plus active FVG soft-exit behavior. Verified with targeted invalidation/action-builder/CLI/exit tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 157 `INTRABAR_SEQUENCING_POLICY_INTEGRATION`; pattern exit simulation now accepts `IntrabarPolicyConfig`, resolves same-candle stop/target ambiguity through reusable policy helpers, preserves conservative default behavior, records ambiguity decision metadata, and documents break-even/trailing sequencing assumptions. Verified with targeted intrabar/exit/action-builder tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 158 `CANDLE_DATA_INTEGRITY_AND_GAP_VALIDATION`; added strict standard candle validation, deterministic UTC timestamp parsing, duplicate/OHLCV/numeric rejection, opt-in interval continuity checks, PostgreSQL provider validation, and canonical strategy-engine validation. Verified with targeted market-data/engine/basic-backtest tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 159 `NO_LOOKAHEAD_PATTERN_DETECTION_CONTRACT`; added public current-index detector helpers across supported patterns, made FVG/Order Block default lifecycle classification signal-time safe, preserved retrospective lifecycle analysis behind explicit config flags, and added rolling-prefix parity/future-contamination tests. Verified with targeted pattern/no-lookahead suites, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 160 `TRANSACTION_COST_AND_SLIPPAGE_REALISM`; wired candle high-low volatility bps into simulated transaction-cost slippage, added per-execution slippage/volatility metadata, summary cost aggregation, zero-cost assumption metadata, and CLI zero-cost warnings. Verified with targeted cost/engine/CLI tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 161 `RISK_PER_TRADE_SIZING_AND_GUARDRAILS`; added `EQUITY_RISK_FRACTION` sizing from `risk_per_unit`, backtest-only guardrail config, max drawdown/consecutive-loss/daily-loss entry blocks with metadata, CLI sizing-mode wiring, and guardrail/sizing tests. Verified with targeted engine/accounting/CLI tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 162 `SHORT_ECONOMICS_MARGIN_FUNDING_LIMITATIONS`; strengthened short-exposure metadata with simulation-only scope, unsupported borrow/funding/maintenance/liquidation fields, CLI short-economics warnings, and README wording. Verified with targeted engine/CLI/execution tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 163 `PERFORMANCE_ATTRIBUTION_METRICS_CUBE`; added lifecycle-based trade attribution metrics, grouped attribution by pattern/direction/side/exit reason/timeframe/session/regime, exposure/turnover outputs, equity drawdown/recovery durations, API contract notes, and regression coverage. Verified with targeted backtesting tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 164 `MARKET_REGIME_AND_INDICATOR_EXPANSION`; added `market_regime.py` with volatility/liquidity/spread/trend/mean-reversion tags, exported the public indicator contract, documented schema/limitations, and added optional strategy-engine execution metadata tagging for regime attribution without changing fills. Verified with targeted indicator/engine tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 165 `PATTERN_SCORE_FEATURE_AUDIT_AND_CALIBRATION`; added common heuristic score metadata helpers, decomposed supported pattern scores into component contributions/sources/placeholders/calibration caveats, propagated score metadata through strategy actions, canonical CLI events, and persistence metadata, and updated pattern explanations. Verified with targeted pattern/strategy/CLI tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 166 `WALK_FORWARD_OOS_MONTE_CARLO_VALIDATION`; added deterministic walk-forward fold generation, fixed-parameter validation runner over the canonical strategy engine, RSI CSV CLI output, aggregate fold distributions, deterministic trade-return bootstrap, schema docs, and regression coverage. Verified with targeted walk-forward/performance/CLI tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 167 `RSI_AND_MEAN_REVERSION_SIGNAL_CONTRACT_REVIEW`; preserved default simple rolling RSI/latest-level behavior, added opt-in Wilder/RMA smoothing, added opt-in crossing trigger mode for RSI strategy/action strategy, documented RSI mean-reversion limitations, and added regression coverage for calculation, warm-up, repeated level behavior, crossing suppression, safety, and BasicBacktester integration. Verified with targeted RSI/basic-backtest tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 168 `RESEARCH_DIAGNOSTICS_PERSISTENCE_AND_API_REPORTING`; made strategy persistence preserve JSON-safe unknown execution diagnostics, carried run metadata into completed-run list models so runtime summaries work for real repository rows, exposed optional read-only `diagnostics` on detail responses, added frontend/API contract typing/docs, and covered new/legacy metadata behavior. Verified with targeted persistence/backend tests, full `pytest`, frontend build, and `git diff --check`; `pytest backend/tests` remains blocked by missing `fastapi` for API route tests.
- 2026-05-24: Completed Task 169 `BACKTEST_REPRODUCIBILITY_RUN_METADATA`; added canonical strategy-run reproducibility metadata with dataset identity, requested/actual candle range, candle-content hash, candle quality summary, deterministic strategy/engine/config hashes, seed slots, and redacted database URL environment fields to CLI JSON and persisted `backtest_runs.metadata`. Verified hash stability, secret redaction, persistence wiring, targeted CLI/persistence tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 170 `EXECUTION_READINESS_SAFETY_BOUNDARY_AUDIT`; added `docs/25_EXECUTION_READINESS_SAFETY_AUDIT.md`, documented that live trading remains blocked, listed missing production execution capabilities and required future guard tasks, linked the audit from README, and recorded follow-up candidates in BACKLOG/STATUS. Verified with execution/risk safety tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Started Task 171 `REFACTOR_DOCUMENTATION_LEDGER_RECONCILIATION`; scope is limited to small behavior-preserving refactors, documentation consistency checks, task checklist verification, fixed 50-task ledger archive reconciliation, status/backlog/history updates, and final verification. Task 138 remains blocked.
- 2026-05-24: Completed Task 171 `REFACTOR_DOCUMENTATION_LEDGER_RECONCILIATION`; extracted shared JSON-safe metadata serialization/hash helpers for strategy persistence and canonical CLI reproducibility metadata, verified completed remediation task checklists, created fixed 50-task ledger archives for Tasks 101-150, reduced root BACKLOG/PROJECT_HISTORY to Tasks 151-171, and kept Task 138 visibly blocked. Verified with targeted persistence/CLI tests, full `pytest`, frontend build, and `git diff --check`.
- 2026-05-24: Created Task 172 `FVG_ACTUAL_FILL_RISK_PLAN_ALIGNMENT` from owner-reported FVG backtest behavior where market fills can occur at actual confirmation-close prices while risk targets remain based on the original FVG midpoint/reference entry. No implementation executed in this creation step.
- 2026-05-24: Completed Task 172 `FVG_ACTUAL_FILL_RISK_PLAN_ALIGNMENT`; pattern action building now rebuilds risk-per-unit and actionable targets from actual entry fill price before exit simulation, filters/sorts targets by long/short direction, preserves original entry reference separately from fill-adjusted risk metadata, and prevents take-profit exits from using stale reference-entry targets that realize losses. Verified with targeted action-builder/strategy-engine/FVG/no-lookahead suites, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 173 `BACKTEST_METRIC_FRONTEND_REPORTING_AND_INTERPRETATION`; added a read-only frontend Performance Diagnostics panel that consumes saved `performance_metrics`, `trade_attribution`, and `cost_summary` metadata, renders risk-adjusted return/trade-quality/cost/exposure/turnover cards with helper text, flags poor risk-adjusted return, high cost drag, low hit rate, negative expectancy, weak drawdown recovery, no completed lifecycle, and zero-cost assumptions, and documents the consumed API metadata paths. Verified with frontend helper tests, frontend build, backend diagnostics service test, full `pytest`, local HTTP response, and `git diff --check`.
- 2026-05-24: Completed Task 174 `BACKTEST_POOR_PERFORMANCE_FORENSIC_DIAGNOSTICS`; added pure `backtest_performance_diagnostics_v1` classification for negative expectancy, low hit rate, poor payoff, high cost drag, drawdown, no completed trades, exposure/turnover, short simulation limits, zero-cost assumptions, fill/reference divergence, negative-PnL take-profit anomaly, and dominant exit reasons; strategy-engine runs now store the diagnostics, backend detail responses compute/expose them read-only for legacy rows, and frontend renders a Run Diagnosis panel. Verified with targeted diagnostics/backend/frontend tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 175 `ENTRY_EXIT_TIMING_FORENSICS_AND_MFE_MAE_METRICS`; added `trade_timing_diagnostics_v1` lifecycle path analytics for direction-aware MFE/MAE, R multiples, bars-to-path events, fill-reference/zone distances, entry-chasing/left-money/immediate-adverse flags, strategy-engine OHLC persistence metadata, backend close-only legacy fallback diagnostics, API contract docs, and a frontend Entry/Exit Timing panel. Verified with targeted timing/engine/backend tests, frontend build/helper tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 176 `FVG_ENTRY_MODE_RETEST_VERSUS_MOMENTUM_EXPERIMENTS`; exposed FVG-only entry-mode CLI controls for market confirmation, next open, entry-reference limit, midpoint limit, boundary limit, and custom-price limit modes; wired max-wait/expiry behavior into canonical action expansion, preserved default market-confirmation behavior, added fill diagnostics and optional read-only entry-mode comparison output with fill rate, trade count, hit rate, average R, expectancy, MFE/MAE, bars waited, and missed-trade count, and documented the FVG economic interpretation. Verified with targeted pattern action/CLI/FVG/entry tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 177 `PATTERN_SPECIFIC_ENTRY_EXIT_POLICY_MATRIX`; added `pattern_execution_policy_v1` for FVG, Order Block, Trendline Break, Cup and Handle, Diamond, and Adam and Eve, enforced CLI pattern/mode compatibility before candle loading, carried policy metadata into strategy parameters, summary diagnostics, and execution metadata, and rendered policy rationale in the frontend Strategy Logic panel. Verified with targeted policy/CLI/action/backend/frontend tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 178 `RISK_REWARD_TARGET_STOP_VALIDITY_AND_DOMINANCE_AUDIT`; added `risk_exit_audit_v1` diagnostics for exit-reason distribution, average PnL/R by exit reason, stop/time/soft-invalidation dominance, target quality, partial-exit contribution, and stop/target direction validation; strategy-engine runs store the audit, backend details expose legacy fallback audits, and frontend renders a Risk Management panel that separates configured risk design from realized outcomes. Verified with targeted risk/backend/frontend tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 179 `CANONICAL_REGIME_GUARDRAIL_AND_CONTINUITY_CLI_WIRING`; added canonical CLI flags for candle-continuity enforcement, market-regime tagging, market-regime windows/minimum trading value, and backtest-only drawdown/consecutive-loss/daily-loss guardrails, passed settings through provider and strategy engine, recorded workflow metadata, and emitted disabled-feature warnings for default compatibility. Verified with targeted CLI/regime/provider tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 180 `COST_PROFILE_PRESETS_AND_SENSITIVITY_REPORTING`; added offline static cost profiles (`zero`, `binance_spot_taker_baseline`, `conservative_crypto_1m`, `high_slippage_stress`), deterministic profile/manual-bps conflict handling with explicit override, cost-profile metadata and approximate cost sensitivity, README/API notes, and frontend cost profile display. Verified with targeted cost/CLI/frontend tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 181 `PATTERN_SCORE_CALIBRATION_ABLATION_AND_THRESHOLDS`; added `pattern_score_calibration_v1` diagnostics with score buckets, hit ratio, expectancy, R metrics, profit factor, placeholder component analysis, observational component ablation deltas, and threshold sensitivity; exposed the report through strategy-engine metadata and read-only backend diagnostics, added the frontend Score Reliability panel, and documented the API contract. Verified with targeted score/backend/pattern tests, frontend build, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 182 `PATTERN_WALK_FORWARD_OOS_VALIDATION_RUNNER`; extended offline WFO validation from RSI-only to pattern strategies with train plus current-test-prefix signal detection, remaining-test-window exit simulation, pattern CLI options for pattern key, entry mode, statuses, score threshold, costs, and sizing, fold-level diagnostics, expectancy/pattern-stability aggregates, and schema documentation. Verified with WFO/no-lookahead/canonical pattern CLI tests, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 183 `FRONTEND_STRATEGY_RISK_ENTRY_EXIT_DEEP_EXPLANATION`; replaced the generic strategy explanation area with read-only selected-run panels for strategy overview, economic hypothesis, indicators, risk design, realized risk behavior, entry timing, exit timing, known limitations, and bad-performance clues; added a typed frontend extraction helper, FVG fill-adjusted-risk/fallback tests, and API contract notes for consumed metadata. Verified with frontend helper tests, frontend build, backend read-only diagnostics service test, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 184 `BAD_RUN_EXPLAINER_AND_RECOMMENDED_NEXT_ANALYSIS_FRONTEND`; added a read-only Run Conclusion panel with deterministic rule mapping from performance, timing, risk, score, cost, and attribution diagnostics into top failure reasons, confidence labels, evidence rows, and recommended next analyses; covered bad/good/legacy fixture behavior and documented consumed API metadata. Verified with frontend helper tests, frontend build, backend read-only diagnostics service test, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 185 `LIQUIDITY_SPREAD_AND_SESSION_FILTER_RESEARCH_FEATURES`; added OHLCV-only tradability proxies for trading-value percentile, liquidity z-score, range-spread percentile, wick dominance, UTC session, and weekday/weekend tags; propagated them through optional engine metadata, added attribution groups by liquidity/spread/weekday/session, documented proxy caveats, and rendered a frontend Tradability Diagnostics panel. Verified with indicator/performance/engine tests, frontend build, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 186 `SAVED_RUN_RESEARCH_REPORT_ARTIFACT`; added a backend read-only `backtest_research_report_v1` builder for saved-run JSON/markdown research notes, recursively redacted obvious secrets and database credentials, exposed the artifact on saved-run detail responses, rendered a frontend Research Report preview, and documented the schema. Verified with report/service/CLI persistence tests, frontend build, full `pytest`, and `git diff --check`.
- 2026-05-24: Completed Task 187 `REFACTOR_DOCUMENTATION_LEDGER_RECONCILIATION_AFTER_BACKTEST_ANALYTICS`; reconciled README/API/status/backlog/history after Tasks 173-186, confirmed the root ledger remains the active Tasks 151-200 window with no premature archive needed, extracted shared frontend value helpers for diagnostics/explanation code, and left the next task undecided with dashboard visual/component testing as the main follow-up candidate. Verified with frontend helper tests, frontend build, full `pytest`, and `git diff --check`.

## Active Historical Notes
- Live trading approval/credential policy/endpoint allowlist/kill-switch design remains unresolved and blocks Task 138.
- Docker runtime verification remains environment-dependent where Docker is unavailable.
- Backend FastAPI route tests require a Python environment with `fastapi` installed.
- 2026-05-24: Ledger range cleanup reconciled root `BACKLOG.md` and `PROJECT_HISTORY.md` with the fixed 50-task archive rule by replacing partial archives with `*_task_051_100.md` archives and keeping root ledgers focused on Tasks 101-148.
- 2026-05-24: Task 171 archived Tasks 101-150 into `docs/ledger_archives/backlog_task_101_150.md` and `docs/ledger_archives/project_history_task_101_150.md`, then reduced root ledgers to a Tasks 151-171 current window.

# Project Backlog (Current Window)

This file keeps a **high-signal recent window** only.
Older items are preserved in fixed 50-task segmented archives:

- `docs/ledger_archives/backlog_task_001_050.md`
- `docs/ledger_archives/backlog_task_051_100.md`
- `docs/ledger_archives/backlog_task_101_150.md`
- `docs/ledger_archives/backlog_task_151_200.md`
- `docs/ledger_archives/backlog_task_201_250.md`
- `docs/ledger_archives/backlog_task_251_300.md`

All items below are candidate/planning pointers unless marked completed.

## Recent Task Window (Tasks 301-350)

- Completed (2026-06-04): Task 330 `LOOKBACK_RETURN_MOMENTUM_V2_RERUN_WITH_1H_INCLUDED_DAILY_REPORT` reran active V2 with `1h`, `4h`, and `1d` all included, then generated a fresh Tistory daily-report artifact. The full `2021-01-01T00:00:00Z` to `2026-06-01T00:00:00Z` native `1h` window still had 7 gaps / 14 missing open times after public backfill attempts stored 0 candles, so the task used the documented common continuous fallback window `2023-03-25T00:00:00Z <= candle time < 2026-06-01T00:00:00Z`. Persisted runs `1217`-`1222`: `1h` variants were negative (`-12.36%`, `-9.09%`), `4h` was mixed (`-4.44%`, `+1.60%`), and `1d` was positive no-cost (`+7.72%`, `+3.38%`). Saved `reports/task_330_v2_1h_included_no_cost_atr1_summary.json`, `reports/TASK_330_LOOKBACK_RETURN_MOMENTUM_V2_RERUN_WITH_1H_INCLUDED_DAILY_REPORT.md`, and `reports/blog_payloads/lookback-return-momentum/v2/20230325-20260601-htf-no-cost-atr1-1h-included/`. No strategy/backtest source-code change, frontend/backend change, live trading behavior, order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-04): Task 329 `REGENERATE_LOOKBACK_RETURN_MOMENTUM_V2_REPORT_WITH_TASK328_WORKFLOW` regenerated the existing `Lookback Return Momentum V2` artifact in place under `reports/blog_payloads/lookback-return-momentum/v2/20210101-20260601-htf-no-cost-atr1/`. Updated `payload.json` with Task 328 presentation notes (`table_purposes`, `chart_purposes`, `required_interpretive_takeaways`, `logic_chain`, and copy checks) and rewrote `report-ko.html` so it keeps V2-style data surfaces while adding V1-style dense interpretation. The report now reclaims data coverage, result comparison, no-cost boundary, exit mix, side attribution, yearly attribution, and representative trades in `해석`, removes report-facing `봅니다.` wording, and treats `1h` as a data coverage limitation rather than a failed strategy result. Existing PNGs were reused after dimension checks. No new backtest, parameter tuning, strategy/code change, DB mutation, candle backfill, frontend/backend change, live trading behavior, order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-03): Task 328 `DAILY_REPORT_DATA_RICH_LOGIC_DENSE_WORKFLOW_REVISION` was created and executed from owner feedback to make future reports use V2-style format/data display while recovering V1-style dense interpretation logic and removing report-facing sentence-final `봅니다.` wording. Updated reusable `docs/blog/daily_report_workflow.md`, `docs/blog/DAILY_REPORT_TEMPLATE.md`, `docs/blog/DAILY_REPORT_STYLE.md`, `docs/blog/backtest_report_data_rules.md`, `docs/blog/agent_handoff_prompt.md`, and `docs/blog/image_generation_prompt.md`. Future `report-ko.html` workflows now require table/image purpose notes, interpretation reclaim of major evidence, success/failure/mixed-result driver analysis, and direct evidence-based Korean wording. Existing report artifacts, payloads, PNGs, backtests, strategy/code, DB records, frontend/backend behavior, live trading behavior, exchange endpoints, secrets, and `.env` files were not edited.
- Completed (2026-06-03): Task 327 `LOOKBACK_RETURN_MOMENTUM_V2_HTF_DAILY_REPORT_ARTIFACT_GENERATION` generated the full Tistory hELLO daily-report artifact from saved Task 326 V2 data only under `reports/blog_payloads/lookback-return-momentum/v2/20210101-20260601-htf-no-cost-atr1/`. Outputs are `payload.json`, `report-ko.html`, `summary_equity_curve.png`, `cost_impact.png`, `representative_win_trade.png`, `representative_loss_trade.png`, `htf_variant_comparison.png`, `yearly_attribution.png`, `exit_mix.png`, `side_attribution.png`, and `data_coverage.png`. The report keeps the title as `Lookback Return Momentum V2`, uses the strategy-level explanation rather than internal experiment labels, records `1h` as a data blocker, and separates positive gross/no-cost `4h`/`1d` evidence from cost-aware viability. Per owner clarification, the higher-timeframe path is now the active report-facing V2 and the older short-timeframe Task 324 V2 draft is superseded for version naming unless a later task retitles or re-versions it. No new backtest, parameter tuning/search, DB mutation, candle backfill, strategy/code change, reusable workflow-doc change, frontend/backend change, live trading behavior, order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-03): Task 326 `LOOKBACK_RETURN_MOMENTUM_HTF_INFORMATION_DELAY_NO_COST_ATR1_VALIDATION` continued after the V2 strategy-document prerequisite. Data preflight passed for native `BTCUSDT` `4h` and `1d` candles over `2021-01-01T00:00:00Z <= candle time < 2026-06-01T00:00:00Z`, but `1h` had 7 internal gaps totaling 14 missing native public kline open times; bounded public backfill attempts stored 0 candles, so the two `1h` variants were recorded as data-preflight blockers instead of being run on incomplete or synthetic data. Persisted no-cost symmetric `1 ATR` runs `1213`-`1216` for the `4h`/`1d` primary grid. `4h_1d_to_12h` returned `-10.40%`, `4h_3d_to_1d` returned `+14.82%`, `1d_1w_to_1d` returned `+8.47%`, and `1d_1m_to_1w` returned `+21.64%` gross/no-cost. Saved `reports/task_326_htf_no_cost_atr1_summary.json` and `reports/TASK_326_LOOKBACK_RETURN_MOMENTUM_HTF_INFORMATION_DELAY_NO_COST_ATR1_VALIDATION.md`. No strategy/code change, frontend/backend change, live trading behavior, order/account/private endpoint behavior, secret, or `.env` change was added.
- Progress (2026-06-03): Task 326 strategy-document prerequisite completed. Created `docs/strategy/lookback_return_momentum_v2.md` from the project strategy-document workflow, documenting the higher-timeframe information-delay hypothesis, `1h`/`4h`/`1d` rationale, no-cost gross diagnostic boundary, symmetric `1 ATR` stop/take-profit geometry, six-variant primary grid, data preflight requirements, Task 324 artifact absence, bounded interpretation rules, implementation notes, and safety boundary. Per the strategy-document rule, execution stopped before data preflight, public candle backfill, implementation/metadata change, DB mutation, backtest execution, compact summary JSON generation, report generation, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change. Remaining Task 326 work requires a later explicit continuation assignment.
- Created (2026-06-03): Task 326 `LOOKBACK_RETURN_MOMENTUM_HTF_INFORMATION_DELAY_NO_COST_ATR1_VALIDATION` from owner request to test whether the information-delay momentum hypothesis is better aligned with higher timeframes than prior `1m`/`5m`/`15m` short-horizon momentum diagnostics. The task defines a future `LOOKBACK_RETURN_MOMENTUM` V2 validation on `BTCUSDT` `1h`, `4h`, and `1d` candles from `2021-01-01T00:00:00Z` to preferred fixed end `2026-06-01T00:00:00Z` exclusive, with no transaction costs, no cost-aware entry filter, and symmetric `1 ATR` stop/take-profit. It predeclares a six-variant primary grid, yearly attribution requirements, strict bounded interpretation rules, and a required report/summary JSON under `reports/`. Owner clarified on 2026-06-03 that this higher-timeframe path is the active report-facing V2; Task 324 is now only a superseded short-timeframe draft unless retitled or re-versioned by a later task. No strategy document creation, implementation, data backfill, DB mutation, backtest execution, report generation, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change was performed during task creation.
- Completed (2026-06-03): Task 325 `BACKFILL_4H_INTERVAL_SUPPORT` added `4h` to the shared Binance public REST kline interval allowlist, making `quant-bitcoin-binance-backfill --interval 4h` and multi-interval inputs containing `4h` valid. README supported interval docs and focused fake-backed market-data tests were updated for downloader validation, backfill request dispatch, interval duration, interval-list parsing, multi-interval runner/CLI dispatch, CLI help, and `4h` continuity. Verification passed (`68` focused market-data tests, `124` full market-data tests, py_compile, diff check, contract grep, and safety grep). No real DB backfill execution, DB mutation, strategy-context wiring, strategy/backtest execution, frontend/backend dashboard change, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change was added. Task 265 remains the broader `1h`/`4h` strategy-context task.
- Created (2026-06-02, superseded for version naming on 2026-06-03): Task 324 `LOOKBACK_RETURN_MOMENTUM_V2_NO_COST_ATR1_EXIT_VALIDATION` from owner request after Task 323 PR #177. The task originally defined a future short-timeframe no-cost ATR-1 validation on `1m`/`5m`/`15m`, but owner clarified that the higher-timeframe Task 326/327 path should be the active report-facing V2. Task 324 must not be executed under the active V2 label unless a later task retitles or re-versions it. No Task 324 implementation, strategy document creation, backtest execution, DB mutation, candle backfill, live trading behavior, exchange endpoint, secret, or `.env` change was performed during task creation.
- Completed (2026-06-02): Task 323 `FRONTEND_LARGE_1M_CANDLE_LOAD_ERROR_FIX` updated the read-only dashboard API contract and backend/frontend graph payload path so large `1m` saved runs can load without fetching/rendering unbounded `graph_points` by default. `GET /api/backtest-runs/{id}` now supports `graph_max_points` and `graph_sampling_mode=preserve_markers`, returns `chart_metadata.graph_points`, and deterministically preserves first/last points, signal/execution markers, trade timestamps when present, nearby marker context as budget allows, and chronological order. The dashboard requests `graph_max_points=3000` by default and displays a sampled-data notice. Verification passed: `pytest backend/tests`, `npm --prefix frontend run typecheck`, `npm --prefix frontend run test:helpers`, `git diff --check`, contract search, and safety search. No strategy/backtest execution, DB mutation, candle backfill, live trading behavior, exchange endpoint, secret, or `.env` change was added.
- Completed (2026-06-02): Task 322 `APPLY_DAILY_REPORT_WORKFLOW_REVISIONS_TO_CURRENT_LOOKBACK_REPORT` revised the current `Lookback Return Momentum V1` Tistory HTML artifact at `reports/blog_payloads/lookback-return-momentum/v1/20260201-20260501-atr-reward-cost/report-ko.html` to apply the completed Task 319-321 daily-report workflow revisions. The report now removes the standalone `가설` section, expands `대표 거래` with integrated diagnostic interpretation, expands `백테스트 설정` with explanatory pseudocode and algorithm mechanics, strengthens `전략에 포함된 가정과 이론적 배경`, and preserves the bounded `해석` that the tested V1 configuration is ineffective after costs under the tested conditions without rejecting momentum strategies generally. Saved Task 311 metrics, colocated PNGs, and same-folder image references were preserved. No payload, new backtest, parameter tuning/search, strategy/code change, DB mutation, candle backfill, reusable workflow-doc edit, image regeneration, live trading behavior, exchange endpoint, secret, or `.env` change was added.
- Completed (2026-06-02): Task 321 `DAILY_REPORT_STRATEGY_ASSUMPTION_THEORY_DEPTH_WORKFLOW_REVISION` updated reusable daily-report workflow guidance so future `전략에 포함된 가정과 이론적 배경` sections explain strategy assumptions, mechanism-level rationale, failure modes, optional formulas, evidence boundaries, and momentum-specific mechanisms such as 과소반응, 느린 포지션 조정, 리스크 프리미엄/tail risk, hedger/speculator structure where relevant, and spot Bitcoin caveats. Existing report artifacts, backtests, strategy/code, DB records, live trading behavior, exchange endpoints, secrets, and `.env` files were unchanged.
- Completed (2026-06-02): Task 320 `DAILY_REPORT_BACKTEST_SETTING_ALGORITHM_EXPLANATION_WORKFLOW_REVISION` updated reusable daily-report workflow guidance so future `백테스트 설정` sections explain important rule/indicator/cost-guard mechanics with plain Korean and concise explanatory pseudocode, including lookback-return momentum entry logic, ATR calculation, no-lookahead candle indexing, indicator warm-up/input/window handling, fill timing, and material exit/cost rules. Existing report artifacts, backtests, strategy/code or indicator implementation, DB records, live trading behavior, exchange endpoints, secrets, and `.env` files were unchanged.
- Completed (2026-06-02): Task 319 `DAILY_REPORT_REPRESENTATIVE_TRADE_DIAGNOSTIC_WORKFLOW_REVISION` updated reusable daily-report workflow guidance so future `대표 거래` sections include integrated diagnostic content covering why the trade happened, entry/exit correctness, strategy-driven versus volatility-driven PnL, performance distortion, recurrence evidence, and possible backtest/fill-logic bug signals without turning those prompts into visible headings, a table of contents, or a checklist. Existing report artifacts, backtests, strategy/code, DB records, live trading behavior, exchange endpoints, secrets, and `.env` files were unchanged.
- Completed (2026-06-02): Task 318 `CURRENT_REPORT_TISTORY_IMAGE_CSS_ONLY_REVISION` applied Task 317's full-width `.section-image` / Tistory image-placeholder CSS behavior to only the current Lookback Return Momentum `report-ko.html` artifact. The CSS now covers `.section-image`, direct images, common Tistory image-like wrappers, and print page-break behavior while preserving the existing hELLO layout. Report narrative/metrics, payloads, PNGs, reusable docs/templates, backtests, strategy/code, DB records, live trading behavior, exchange endpoints, secrets, and `.env` files remained unchanged.
- Completed (2026-06-02): Task 317 `TISTORY_IMAGE_EMBED_AND_HYPOTHESIS_OMISSION_WORKFLOW_REVISION` updated reusable `docs/blog` workflow/template/style/handoff/data/image rules and `docs/blog/report_template.html` for Tistory publishing. Future report images use a full-width `.section-image` wrapper that supports owner-side `[##_Image|...|alignCenter|width="100%"|_##]` reinsertion, and final daily reports omit standalone `가설` / hypothesis sections by default. Existing report artifacts, images, backtests, strategy/code, DB records, live trading behavior, exchange endpoints, secrets, and `.env` files remained untouched.
- Completed (2026-05-31): Task 301 `LOOKBACK_RETURN_MOMENTUM_DAILY_REPORT_GENERATION` generated a publish-ready Korean daily report from the saved `LOOKBACK_RETURN_MOMENTUM` validation results. The colocated artifact is stored at `reports/blog_payloads/lookback-return-momentum/v1/20260520-20260528/` with `payload.json`, `report-ko.md`, `summary_equity_curve.png`, `cost_impact.png`, `representative_win_trade.png`, and `representative_loss_trade.png`. The report states that `1m` and `15m` were negative and cost-dominated, that `5m` was skipped because local closed candles were missing, and that results are research-only. No new backtest execution, parameter tuning/search, strategy/code change, candle backfill, saved-run DB mutation, frontend/backend/API change, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-05-31): Task 302 `LOOKBACK_RETURN_MOMENTUM_REPORT_COPY_READABILITY_REVISION` revised the existing `Lookback Return Momentum V1` report under `reports/blog_payloads/lookback-return-momentum/v1/20260520-20260528/`. `report-ko.md` now replaces vague "성과가 약하다" phrasing with concrete saved-result descriptions, uses compact tables for dense metrics, removes awkward English micro-headings, and keeps same-folder image references. `payload.json` was updated only for the matching cost-impact interpretation sentence. No new backtest execution, parameter tuning/search, strategy/code change, candle backfill, saved-run DB mutation, image generation, frontend/backend/API change, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-01): Task 303 `DAILY_REPORT_INTERPRETATION_STYLE_WORKFLOW_REVISION` updated the reusable daily-report template, style guide, workflow, agent handoff prompt, payload data rules, and image prompt so future Korean reports use interpretation-centered structure and natural wording. The docs now tell report writers to omit absent pattern/filter/artifact mentions by default, move unavailable interval coverage such as missing `5m` local candles to limitations or next improvements, enrich representative trade descriptions with available context, and replace generic final conclusions with an interpretation section that covers experiment intent, observed result, likely causes, and next improvements. No report artifact rewrite, backtest execution, parameter tuning/search, strategy/code change, candle backfill, DB mutation, image generation, frontend/backend/API change, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-01): Task 305 `LOOKBACK_RETURN_MOMENTUM_5M_COST_AWARE_RR_REVISION` updated the momentum strategy document, implemented `cost_aware_entry_filter_v1` for `LOOKBACK_RETURN_MOMENTUM`, wired existing cost-aware CLI settings into the momentum runner path, added focused strategy/runner/persistence tests, verified complete `BTCUSDT` `1m`/`5m`/`15m` local candles over `2026-02-01T00:00:00Z <= candle time < 2026-05-01T00:00:00Z`, and saved revised validation report `reports/TASK_305_LOOKBACK_RETURN_MOMENTUM_5M_COST_AWARE_RR_REVISION.md`. No new backfill was needed. Saved runs `1162`/`1163`/`1164` all had every candidate blocked by `COST_INFEASIBLE_NET_RR` (`1m`: 79,981, `5m`: 17,171, `15m`: 5,937), so no fills occurred and realized return stayed `0.0%`. No parameter tuning/search, frontend/backend/API change, live trading behavior, exchange order/account/private endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-01): Task 306 `LOOKBACK_MOMENTUM_REPORT_OWNER_FEEDBACK_HTML_TEMPLATE_REVISION` rewrote the existing Lookback Return Momentum Korean report using the owner's feedback and `docs/blog/report_template.html`. The report now opens with a plain-language strategy explanation, removes awkward opening/default-value/disclaimer phrasing, removes `주의해서 볼 점`, retitles the final section to `해석`, uses simple bullets, strengthens the broader strategy-level theoretical/economic background, and interprets the observed negative result through saved evidence such as gross-vs-net PnL, exit mix, turnover, cost drag, and reward/risk structure. `payload.json` was updated only for matching report-facing narrative fields. No backtest execution, strategy/code change, DB mutation, image generation, live trading behavior, exchange endpoint behavior, secret, or `.env` change was performed.
- Completed (2026-06-01): Task 307 `DAILY_REPORT_WORKFLOW_OWNER_FEEDBACK_RULE_REVISION` updated reusable `docs/blog` workflow/template/style/handoff/data/image rules rather than patching a specific report. Future full-report artifacts now default to `report-ko.html`, use `docs/blog/report_template.html` as the HTML layout/reading-flow reference, and no longer define Markdown as the default final report output. The workflow now requires a strategy-first opening, avoids awkward `기본값` labels and old English micro-headings, removes the default caution section, uses a final `해석` section with evidence-supported success/failure drivers, and strengthens theory/economic background guidance. Existing `reports/**/report-ko.md` and `reports/**/payload.json` were not edited by Task 307.
- Completed (2026-06-01): Task 308 `LOOKBACK_RETURN_MOMENTUM_LOWER_ENTRY_THRESHOLD_BACKTEST_DAILY_REPORT` executed a predeclared lower `entry_threshold` grid for `LOOKBACK_RETURN_MOMENTUM` on `BTCUSDT` `1m`/`5m`/`15m` over `2026-02-01T00:00:00Z <= candle time < 2026-05-01T00:00:00Z`, with Task 305's cost-aware entry gate preserved. Persisted runs `1168`-`1179`; raw candidates increased at the lowest thresholds (`1m` +34.40%, `5m` +28.69%, `15m` +25.47%), but accepted entries stayed `0` and every candidate was blocked by `COST_INFEASIBLE_NET_RR`. Generated `reports/TASK_308_LOOKBACK_RETURN_MOMENTUM_LOWER_ENTRY_THRESHOLD_BACKTEST_DAILY_REPORT.md` and a colocated daily-report artifact under `reports/blog_payloads/lookback-return-momentum/v1/20260201-20260501-lower-threshold/` with `payload.json`, `report-ko.html`, and four PNGs.
- Completed (2026-06-01): Task 309 `LOOKBACK_RETURN_MOMENTUM_ATR_RISK_EXIT_REVISION` updated the momentum strategy document before implementation, replaced the primary risk distance with `ATR_at_entry`, defaulted stop-loss and take-profit distances to `1 ATR`, preserved explicit fixed-percent mode for old diagnostics/tests, added invalid ATR diagnostics, wired ATR settings into CLI/config/metadata, and persisted Task 309 validation runs `1180`-`1191` using the Task 308 threshold grid. All accepted entries remained `0`; `1m` had no invalid ATR blocks, `5m` had one early invalid ATR block per variant, and `15m` had three early invalid ATR blocks per variant. All other candidates were blocked by `COST_INFEASIBLE_NET_RR` because symmetric `1 ATR` stop/target geometry cannot satisfy `min_net_rr=1.0` after positive estimated round-trip cost. Saved report: `reports/TASK_309_LOOKBACK_RETURN_MOMENTUM_ATR_RISK_EXIT_REVISION.md`.
- Completed (2026-06-01): Task 310 `TISTORY_DAILY_REPORT_WORKFLOW_STYLE_REVISION` updated reusable `docs/blog` templates/style/workflow/data/handoff/image rules plus `docs/strategy` strategy-document workflow rules, without editing existing report artifacts. Future Tistory HTML reports now target a `1392px * 708px` primary desktop review plus responsive behavior, use strategy/version-centered titles, stable strategy-description leads, version-change summaries in `핵심요약`, visible bullets, purposeful tables, stronger theory/reference sourcing from strategy docs, folded strategy-rule content inside the theory/background section, preserved `백테스트 설정`, reasoned improvement wording, and prohibited awkward wording such as `그것은`.
- Completed (2026-06-01): Task 311 `LOOKBACK_RETURN_MOMENTUM_ATR_REWARD_COST_GEOMETRY_DIAGNOSTIC` updated the momentum strategy document before implementation, added configurable `minimum_atr_bps` support plus `ATR_TOO_SMALL_FOR_COST` diagnostics, wired `--minimum-atr-bps` into the runner, and executed the predeclared 18-run grid over `BTCUSDT` `1m`/`5m`/`15m` for the February-to-May window. Persisted runs `1192`-`1209` showed that asymmetric targets made entries cost-feasible (`1m` from `2.5 ATR`, `5m`/`15m` from `2.0 ATR`), but all filled variants were net negative after costs. `minimum_atr_bps=20.0` reclassified low-ATR rejected candidates but did not change accepted trades. Report saved at `reports/TASK_311_LOOKBACK_RETURN_MOMENTUM_ATR_REWARD_COST_GEOMETRY_DIAGNOSTIC.md`.
- Completed (2026-06-02): Task 312 `LOOKBACK_MOMENTUM_TASK311_DAILY_REPORT_GENERATION` generated the Tistory-ready daily report from saved Task 311 results only. The colocated artifact is stored at `reports/blog_payloads/lookback-return-momentum/v1/20260201-20260501-atr-reward-cost/` with `payload.json`, `report-ko.html`, `summary_equity_curve.png`, `cost_impact.png`, `reward_cost_geometry.png`, `accepted_entries_by_variant.png`, `representative_win_trade.png`, and `representative_loss_trade.png`. The report uses a strategy/version-centered title, explains the ATR reward/cost geometry, preserves Task 311 metrics, and avoids internal task/run identifiers in reader-facing prose. No new backtest, strategy/code change, DB mutation, live trading behavior, exchange endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-02): Task 313 `DAILY_REPORT_HELLO_SKIN_INTERPRETATION_WORKFLOW_REVISION` updated reusable `docs/blog` workflow/style/template/data/image/handoff rules and `docs/blog/report_template.html`. Future Tistory hELLO reports now use a single-file HTML layout with `1120px` default centered `.report-page`, mobile-safe body width, left-first table alignment, full-width image display, larger chart dimension guidance, and `해석` rules that separate bounded tested-version conclusions from broader strategy-family claims. The workflow now requires explaining why broad rejection is premature when evidence is limited, and also prevents overclaiming positive results beyond the tested scope. Existing report artifacts, backtests, strategy/code changes, DB mutation, live trading behavior, exchange endpoints, secrets, and `.env` changes were out of scope and were not edited by Task 313.
- Completed (2026-06-02): Task 314 `REGENERATE_TASK312_REPORT_WITH_HELLO_INTERPRETATION_RULES` regenerated the existing Task 312 report artifact in place under `reports/blog_payloads/lookback-return-momentum/v1/20260201-20260501-atr-reward-cost/` from saved Task 311 outputs. The regenerated `report-ko.html` uses the Task 313 Tistory hELLO layout contract with `1120px` centered `.report-page`, mobile-safe width, left-first tables, full-width images, and a bounded `해석` section that distinguishes the current V1 failure after costs from rejecting momentum strategies generally. `payload.json` narrative fields and same-folder PNGs were refreshed, and payload metrics were verified against the Task 311 manifest. No new backtest, parameter tuning/search, strategy/code change, DB mutation, live trading behavior, exchange endpoint behavior, secret, or `.env` change was added.
- Completed (2026-06-02): Task 315 `DAILY_REPORT_IMAGE_GENERATION_RULE_CONSISTENCY_REVISION` updated reusable `docs/blog` image-generation workflow/rule docs so future report images use stable canvas sizes, no-crop/aspect-ratio-preserving resizing with padding, representative trade charts with surrounding pre-entry and post-exit context, the `20260520-20260528` representative win trade reference style, annotation bands for dense metrics, and label-overlap QA. `docs/blog/report_template.html` was preserved, and existing report artifacts/PNGs were not regenerated or edited by Task 315.
- Completed (2026-06-02): Task 316 `REGENERATE_LOOKBACK_MOMENTUM_REPORT_IMAGES_AND_COPY_RULE_REVISION` regenerated the six current Lookback Return Momentum V1 report PNGs in place under Task 315 image rules. The refreshed charts use no-crop 1800px-wide canvases, annotation bands, stable dimensions, and representative win/loss trade charts with surrounding `15m` candle context. The current `report-ko.html` and reusable `docs/blog` rules now replace awkward `더 강한 결론` / `강한 결론` phrasing with wording about expanding the judgment scope to the broader strategy family. Payload metrics were verified against the saved Task 311 manifest. No new backtest, strategy/code change, DB mutation, candle backfill, live trading behavior, exchange endpoint behavior, secret, or `.env` change was added.

## Important Blocked Work

- Blocked: Task 138 `GUARDED_BINANCE_SPOT_LIVE_EXECUTION_WITH_OWNER_APPROVAL` remains blocked pending explicit owner approval for live order execution and the live-readiness prerequisites documented in `docs/25_EXECUTION_READINESS_SAFETY_AUDIT.md`.

## Current Candidates / Follow-ups

- Completed follow-up: Task 330 reran V2 with `1h` included by using the documented common continuous `1h`/`4h`/`1d` fallback window after full-window native `1h` repair failed.
- Follow-up candidate: create a cost-aware `LOOKBACK_RETURN_MOMENTUM V2` validation task for the positive daily-horizon Task 330 variants, especially `1d_1w_to_1d`, before making any viability claim from the gross/no-cost daily report.
- Follow-up candidate: create a predeclared regime-attribution task for V2 using objective labels such as trend state, volatility percentile, drawdown/recovery state, liquidity, and risk-on/risk-off proxy. This should evaluate the same variants before adding new filters or tuning.
- Follow-up candidate: create a separate bounded data-backfill execution task if the owner wants Codex to populate PostgreSQL with actual `1h`, `4h`, or `1d` candles after Task 298/325 code support.
- Follow-up candidate: create a separate report-conversion task if the owner wants an existing Markdown report artifact converted to `report-ko.html` using the revised workflow.
- Follow-up candidate: create a bounded `LOOKBACK_RETURN_MOMENTUM` trade-quality diagnostic for accepted entries after Task 311. Prefer testing a stronger ATR floor derived from the cost formula, directional continuation confirmation, or time-of-day/liquidity filtering before increasing target multiples further.
- Follow-up candidate: create a separate gross-vs-net diagnostic task if the owner wants to observe filled trades with the cost-aware entry gate disabled or relaxed for comparison only.
- Follow-up candidate: implement Task 265 `HIGHER_TIMEFRAME_1H_4H_BACKFILL_AND_STRATEGY_CONTEXT` if the owner wants stored `1h`/`4h` candles wired into strategy backtests after the narrower Task 325 backfill interval support.
- Follow-up candidate: create a separate locked OOS/WFO diagnostic task before making any further claim about `LOOKBACK_RETURN_MOMENTUM`; Task 299 results are cost-dominated and research-only.
- Follow-up candidate: implement Task 288 `REPAIRED_0420_FORWARD_NEW_MODEL_DEVELOPMENT` if the owner assigns it; it must predeclare model families/windows/costs, persist decision-driving runs, enforce minimum trade-count/cost-stress gates, and keep any passing result research-only pending future unseen data.
- Follow-up candidate: create a future reusable report-payload/image exporter task if the owner wants saved backtest runs to automatically emit colocated `payload.json` and required PNGs without report markdown, image-plan files, or an `images/` subdirectory.
- Follow-up candidate: implement Task 272 `ORDER_BLOCK_COST_AWARE_RR_ENTRY_GUARD` to enforce fee-adjusted reward/risk before `ORDER_BLOCK` entries.
- Follow-up candidate: `LIVE_EXECUTION_KILL_SWITCH_AND_MAX_NOTIONAL_GUARDS` (prerequisite before any future live execution task).
- Follow-up candidate: `LIVE_EXECUTION_SYMBOL_FILTER_AND_STALE_DATA_PRECHECKS` (exchange filter, stale candle, and clock-skew checks before live intent submission).
- Follow-up candidate: `LIVE_EXECUTION_IDEMPOTENCY_AND_RESTART_RECONCILIATION` (durable duplicate-order prevention and restart recovery).
- Follow-up candidate: `LIVE_EXECUTION_CANCEL_REPLACE_AND_PARTIAL_FILL_POLICY` (cancel/replace, timeout, orphan-order, and partial-fill handling).
- Follow-up candidate: `LIVE_EXECUTION_MONITORING_ALERTING_AND_SECRET_POLICY` (alerts, credential storage/rotation, redaction, and operational readiness).
- Follow-up candidate: add visual regression/UI tests for dashboard marker/table/channel-overlay rendering once a frontend test harness is assigned.

## Backend/Frontend Candidates

- Follow-up candidate: add a dedicated frontend unit/component test harness for dashboard helper/rendering behavior if future UI work continues.
- API contract evolution for any future backend/frontend coordination beyond the current read-only dashboard.
- Backend API extensions only when assigned by a future backend task.
- Frontend dashboard extensions only when assigned by a future frontend task.

## Deferred Verification

- Local Docker runtime verification for previously completed Docker-related tasks in a Docker-capable environment.
- Backend FastAPI route tests require a Python environment with `fastapi` installed.

## Ledger Maintenance

- Completed (2026-05-24): Reconciled root backlog archive pointers to fixed 50-task ranges and moved Tasks 088-100 entries into `docs/ledger_archives/backlog_task_051_100.md`.
- Completed (2026-05-24): Task 171 created `docs/ledger_archives/backlog_task_101_150.md` and reduced root backlog to the Tasks 151-171 recent window.
- Completed (2026-05-24): Task 225 created `docs/ledger_archives/backlog_task_151_200.md` and reduced root backlog to the Tasks 201-225 recent window.
- Completed (2026-05-28): Created `docs/ledger_archives/backlog_task_201_250.md` and reduced root backlog to the Tasks 251-300 recent window when Task 251 was created.
- Completed (2026-05-31): Created `docs/ledger_archives/backlog_task_251_300.md` and reduced root backlog to the Tasks 301-350 recent window when Task 301 was created.

- Completed (2026-06-01): Task 304 `REWRITE_LOOKBACK_MOMENTUM_DAILY_REPORT_WITH_REVISED_PROMPT` rewrote the existing Lookback Return Momentum Korean daily report with the revised Task 303 prompt/workflow/style guidance, preserving saved metrics, same-folder image references, payload semantics, and research-only framing.

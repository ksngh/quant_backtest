[project]
name = "quant-backtest-api-placeholder"
version = "0.0.0"
description = "Placeholder backend package metadata for Task 074 directory boundaries."
requires-python = ">=3.10"

[tool.task074]
note = "Placeholder only. Final backend packaging decision is deferred to a later implementation task."
